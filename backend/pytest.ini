[pytest]
asyncio_mode = auto
testpaths = tests
# The browser journeys need a running server and a live model, so they are not
# part of the default run. They skip themselves unless E2E_OWNER_PASSWORD is set.
norecursedirs = tests/e2e
